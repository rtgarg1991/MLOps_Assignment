apiVersion: batch/v1
kind: Job
metadata:
  name: manual-promote-job
spec:
  ttlSecondsAfterFinished: 600
  template:
    spec:
      serviceAccountName: mlops-trainer
      containers:
      - name: promoter
        image: us-central1-docker.pkg.dev/mlops-final-001/mlops-repo/heart-trainer:manual-test-v1
        imagePullPolicy: Always
        command: ["python", "src/promote.py"]
        args:
          - "--bucket=mlops-final-001-mlops-artifacts"
          - "--pr-number=999"
      restartPolicy: Never